createUserTable:
CREATE TABLE IF NOT EXISTS User  (
  id TEXT NOT NULL PRIMARY KEY,
  user_name TEXT NOT NULL
);

deleteUserTable:
DROP TABLE IF EXISTS User;

insertUser:
INSERT OR REPLACE INTO User(id, user_name)
VALUES ?;

deleteUser:
DELETE
FROM User
WHERE id = ?;

selectAll:
SELECT *
FROM User;

